package models

const NotAllowed = "Not allowed"

const JsonError = "Error json format"

const BcryptError = "Invalid password"

const TakenClient = "Phone is already register"

const InvalidClient = "Invalid password or phone"

const InvalidTokens = "Invalid tokens"

const InvalidId = "Invalid ID"
